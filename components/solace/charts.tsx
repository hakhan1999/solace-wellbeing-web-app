'use client';
import {ResponsiveContainer,AreaChart,Area,CartesianGrid,XAxis,YAxis,Tooltip,BarChart,Bar,Legend} from 'recharts';
import {useSolace} from '@/lib/solace/store';
import {comparisons} from '@/lib/solace/data';
import type {Store} from '@/lib/solace/data';
export function OutcomeChart({engagementId,department='All departments',employeeId='all',category='All categories',from='',to='',bars=false,source}:{source?:Store;engagementId?:string;department?:string;employeeId?:string;category?:string;from?:string;to?:string;bars?:boolean}){const {data}=useSolace();const values=comparisons(source||data,engagementId,department,employeeId,category,from,to);return <><div className="chart" role="img" aria-label="Fictional wellbeing index comparison, zero to one hundred"><ResponsiveContainer width="100%" height="100%">{bars?<BarChart data={values} barGap={5}><CartesianGrid vertical={false} stroke="#edf0ef"/><XAxis dataKey="name" axisLine={false} tickLine={false} tick={{fontSize:12,fill:'#6c7d79'}}/><YAxis domain={[0,100]} axisLine={false} tickLine={false} width={32} tick={{fontSize:12,fill:'#6c7d79'}}/><Tooltip/><Legend/><Bar dataKey="Baseline" fill="#d3e0d9" radius={[4,4,0,0]}/><Bar dataKey="Current" fill="#21836e" radius={[4,4,0,0]}/><Bar dataKey="Final" fill="#e3b879" radius={[4,4,0,0]}/></BarChart>:<AreaChart data={values}><defs><linearGradient id="chartFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#268a72" stopOpacity={.15}/><stop offset="100%" stopColor="#268a72" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="#edf0ef"/><XAxis dataKey="name" axisLine={false} tickLine={false} tick={{fontSize:12,fill:'#6c7d79'}}/><YAxis domain={[0,100]} axisLine={false} tickLine={false} width={32} tick={{fontSize:12,fill:'#6c7d79'}}/><Tooltip/><Area type="monotone" dataKey="Baseline" stroke="#b6c9bf" fill="none" strokeWidth={2} strokeDasharray="5 5"/><Area type="monotone" dataKey="Current" stroke="#25856e" fill="url(#chartFill)" strokeWidth={3}/></AreaChart>}</ResponsiveContainer></div><div className="sr-only">{values.map(v=><p key={v.name}>{v.name}: baseline {v.Baseline??'no results'}, current {v.Current??'no results'}, final {v.Final??'no results'}.</p>)}</div></>}
